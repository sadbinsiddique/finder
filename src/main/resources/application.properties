spring.application.name=Finder

# Database Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/finder?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password

spring.jpa.hibernate.ddl-auto=update
# run one `create` -> then switch to `update` or `validate`